<h1><%=title%></h1>
<div id="posts"></div>
<!-- 게시판 템플릿 -->
<script id="temp" type="x-handlebars-template">
    <table class="table table-striped table-hover">
        {{#each .}}
            <tr>
                <td>{{id}}</td>
                <td>{{title}}</td>
            </tr>
            <tr>
                <td colspan=2>{{body}}</td>
            </tr>
        {{/each}}
    </table>
</script>
<!-- <div style="text-align: center;">
    <button id="prev" class="btn btn-info">이전</button>
    <span id="page"></span>
    <button id="next" class="btn btn-success">다음</button>
</div> -->
<script>
    // let page = 1;

    // $("#next").on("click", function(){
    //     page++;
    //     getList();
    // });

    // $("#prev").on("click", function(){ /* $ 제이쿼리 선언 index*/
    //     page--;
    //     getList();
    // });

    getList();
    function getList() {
        fetch('https://jsonplaceholder.typicode.com/posts')
            .then(response => response.json())
            .then(json => {
                console.log(json);
                const temp = Handlebars.compile($("#temp").html());
                $("#posts").html(temp(json));
                // const total = json.length;
                // const last = Math.ceil(total/10);
                
                // //$("#page").html(page);
                // $("#page").html(`<b>${page}</b>`);

                // let start = (page-1)*10 + 1;
                // let end = page*10;
                // // $("#posts").html(""); //데이터초기화

                // let str = "";
                // str += `<table class="table table-striped table-hover">`;
                // $(json).each(function() { //JSON 데이터의 각 요소에 대해 반복합니다
                //     const title = this.title; //변수 저장
                //     const id = this.id;
                //     if(id>=start && id<=end) { //각 포스트의 ID가 시작 페이지와 끝 페이지 사이에 있는지를 확인하고, 그렇다면 해당 포스트의 제목과 ID를 페이지에 추가합니다.
                //         //$("#posts").append(`${id} : ${title}<hr>`);
                //         str += `<tr>`;
                //         str += `<td>${id}</td>`;
                //         str += `<td>${title}</td>`;
                //         str += `</tr>`;

                //     }
                // });
                // str += `</table>`;
                // $("#posts").html(str);

                // if(page==1) {
                //     $("#prev").attr("disabled", true);
                // }
                // else {
                //     $("#prev").attr("disabled", false);
                // }

                // if(page==last) {
                // $("#next").attr("disabled", true);
                // }
                // else {
                //     $("#next").attr("disabled", false);
                // }
            });
    }
</script>