<h1><%=title%></h1>
<div id="todos">데이터 없음</div>
<!-- 할일목록 템플릿 -->
<script id="temp" type="x-handlebars-template"> 
    <table class="table table-striped table-hover">
        {{#each .}}
            <tr>
                <td>{{completed}}</td>
                <td>{{id}}</td>
                <td>{{title}}</td>
            </tr>
        {{/each}}
    </table>
</script>
<script>
    getTodos(); //함수 호출
    function getTodos() { //함수 정의
        fetch(' https://jsonplaceholder.typicode.com/todos')
        .then(response => response.json())
        .then(json => {
            console.log(json)
            const temp=Handlebars.compile($("#temp").html());
            $("#todos").html(temp(json));
            // let str = '';
            // str += `<table class="table table-striped table-hover">`;
            // $(json).each(function(){
            //     const title = this.title;
            //     const id = this.id;
            //     str += `<tr>`;
            //     str += `<td>${id}</td>`;
            //     str += `<td>${title}</td>`;
            //     str += `</tr>`;    
            // })
            // str += `</table>`;
            //$("#todos").html(str);
        })
    }
</script>